ol.breadcrumb
  li
    a href="/admin/home"  Inicio
  li
    a href="/groups"  Grupos
  li.active Alta de Grupos
h1 class="center" Alta de Grupo
div class="container well"
  = simple_form_for @group do |f|
    = f.association :timetable, label_method: :interpolate_day_time, label: "Horario"
    = f.association :instructor, label_method: :full_name, label: "Instructor", collection: User.all.where(role: 2)
    = f.input :seats, label: "Lugares", input_html: { min: 1 }
    = f.input :classroom, label: "Salón"
    div class="center"
    	= f.button :submit, value: "Aceptar"
